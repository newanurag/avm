How to build or make the project

1) . ./setup.sh # This will setup paths and other variables into include.mk
2) make all pkg # This will generate kernel modules and generate a rpm package in packaging folder
3) make pkg # This will generate an rpm package


How to build individual engines e.g. dedupe_engine or metadata_engine
1) . ./setup.sh
2) make dedupe
